# -----------------------------------------------------------------------------
# Libvirt: networking
# The VM attaches to an existing libvirt network via var.libvirt_network_name
# (see libvirt_domain.couchbase in vm.tf). No separate network resource is
# created here.
# -----------------------------------------------------------------------------

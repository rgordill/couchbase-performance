# Secrets and credentials
*.key
*.pem
*.crt
*-credentials.yaml
secrets/
**/vault.yml
**/secrets.yml
*.vault

# Backup files
*.bak
*.backup
*~

# OS files
.DS_Store
Thumbs.db

# IDE files
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log

# Temporary files
tmp/
temp/
*.tmp

# ArgoCD local state
.argocd/

# Helm cache (used by deploy-manual.sh when home cache is not writable)
argocd/.helm/

# Performance test results
results/*.json
results/*.csv
!results/.gitkeep

# Terraform (vm/terraform/libvirt and vm/terraform/aws)
vm/terraform/libvirt/.terraform/
vm/terraform/libvirt/*.tfstate
vm/terraform/libvirt/*.tfstate.*
vm/terraform/libvirt/*.tfvars
!vm/terraform/libvirt/*.tfvars.example
vm/terraform/aws/.terraform/
vm/terraform/aws/*.tfstate
vm/terraform/aws/*.tfstate.*
vm/terraform/aws/*.tfvars
!vm/terraform/aws/*.tfvars.example

# Ansible runtime (vm/ansible/)
vm/ansible/.ansible/

# Environment specific overrides (should be in git but flagging for review)
# environments/*/secrets.yaml

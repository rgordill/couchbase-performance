---
# ApplicationSet: one Application for the in-cluster (prometheus-server).
# Cluster secret has annotation ingress-domain. Host prometheus-server.<ingress-domain> is
# injected via source.kustomize.patches into the Ingress.
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: prometheus-server
  namespace: openshift-gitops
spec:
  project: couchbase-performance
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - clusters:
        selector:
          matchLabels:
            argocd.argoproj.io/cluster-name: in-cluster
  template:
    metadata:
      name: 'prometheus-server'
      namespace: openshift-gitops
      annotations:
        argocd.argoproj.io/sync-wave: "2"
        ingress-host: 'prometheus-server.{{ index .metadata.annotations "ingress-domain" }}'
    spec:
      project: couchbase-performance
      source:
        repoURL: https://github.com/rgordill/couchbase-performance.git
        targetRevision: HEAD
        path: argocd/manifests/prometheus-server
        kustomize:
          patches:
            - target:
                kind: Ingress
                name: prometheus-server
              patch: |-
                - op: replace
                  path: /spec/rules/0/host
                  value: "prometheus-server.{{ index .metadata.annotations "ingress-domain" }}"
                - op: replace
                  path: /spec/tls/0/hosts/0
                  value: "prometheus-server.{{ index .metadata.annotations "ingress-domain" }}"
      destination:
        server: '{{.server}}'
        namespace: couchbase
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
          allowEmpty: false
        syncOptions:
          - ServerSideApply=false
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m

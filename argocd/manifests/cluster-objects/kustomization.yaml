# Cluster objects for app-of-apps (source 1): project, RBAC, PreSync hook.
# PreSync creates Argo CD in-cluster Secret (ingress subdomain) for ApplicationSet and grafana-server PostSync.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - project.yaml
  - rbac-cluster-config-hook.yaml
  - sync-hook-cluster-config-pre.yaml

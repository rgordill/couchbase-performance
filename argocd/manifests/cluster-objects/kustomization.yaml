# Cluster objects for app-of-apps (source 1): project, RBAC, PreSync hook.
# PreSync creates ConfigMap cluster-configuration (ingress subdomain) before applications sync.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - project.yaml
  - rbac-cluster-config-hook.yaml
  - sync-hook-cluster-config-pre.yaml
